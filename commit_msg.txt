feat: add site header and footer components

- Implement SiteFooter component with navigation links and newsletter subscription form.
- Implement SiteHeader component with dynamic navigation based on categories and user authentication.
- Create JoinNewsletterForm component for newsletter subscriptions with validation and loading state.

chore: add environment loader for database configuration

- Create env-loader.ts to load environment variables from .env files.

chore: seed database with initial data

- Implement seed script to populate categories, products, customers, and orders in the database.

feat: add custom hooks for debouncing and media queries

- Implement useDebounce hook for managing debounced state.
- Implement useMediaQuery hook for responsive design handling.

feat: add product validation schemas

- Create validation schemas for product creation, updating, and filtering using zod.

fix: resolve Next.js 'use server' action re-export compilation errors and fix build
